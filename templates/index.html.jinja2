<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    </title>
            <link rel="stylesheet" href="static/main.css">
            <link href="https://fonts.googleapis.com/css?family=Merriweather:300,300i,400,400i,700,700i" rel="stylesheet">
</head>
<body>
    <header>
            <h1>{{ title }}</h1>
    </header>
    <main>
        <section>
            {% if articles %}
                {% for a in articles %}
                        <div class="item">
                            <h3>{% if a.link %}<a href="{{ a.link }}">{{ a.title or 'Untitled' }}</a>{% else %}{{ a.title or 'Untitled' }}{% endif %}</h3>
                                <p class="meta">
                                    {% if a.feed_title %}
                                        <span class="feed">{{ a.feed_title }}</span>
                                    {% endif %}
                                    {% if a.feed_title and a.published %}
                                        <span class="sep">|</span>
                                    {% endif %}
                                    {% if a.published %}
                                        <span class="date"><a href="#">{{ a.published }}</a></span>
                                    {% endif %}
                                </p>
                            <p class="excerpt">
                                {% if a.content %}
                                    {{ a.content }}
                                {% elif a.raw and a.raw.get('summary') %}
                                    {{ a.raw.get('summary') }}
                                {% else %}
                                    -/-
                                {% endif %}
                            </p>
                        </div>
                {% endfor %}
            {% else %}
                <p>No articles found.</p>
            {% endif %}
        </section>
    </main>
    <footer>
        <p>&copy; 2023-{{ year or 2025 }} {{ title }} | Based on a template <a href="https://werd.io" rel="me">by Ben Werdmuller</a> | Updated {{ build_time }}</p>
    </footer>
</body>
</html>