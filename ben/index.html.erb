<!doctype html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
      Recent Education News
    </title>
    <style>
      body {
        background: #fff;
        width: 85%;
        max-width: 760px;
        margin: auto;
        font-family: Merriweather,serif;
      }
      a {
        text-decoration: none;
        color: #000;
        font-weight: 400;
      }
      h1 {
        margin: 0;
        margin-top: 2em;
      }
      h3 {
        font-weight: 400;
        margin-bottom: 0;
        padding-bottom: 0;
      }
      p {
        margin-top: 0.5em;
        border-bottom: 1px #ccc solid;
        padding-bottom: 0.5em;
        font-size: 0.8em;
      }
      p a {
        color: #999;
      }
      p a:hover {
        color: #666;
      }
      .time {
        border: 0;
        margin-bottom: 3em;
      }
      footer {
        margin-top: 3em;
        margin-bottom: 3em;
        font-size: 0.8em;
      }
    </style>
    <link href="https://fonts.googleapis.com/css?family=Merriweather:300,300i,400,400i,700,700i" rel="stylesheet">
    <script data-goatcounter=https://ID59js8gme0.goatcounter.com/count async src=/js/count.js></script>
  </head>
  <body>
    <h1>Recent Education News</h1>
    <p class="time">
      <em>The latest education papers. Last updated <%= Time.current.in_time_zone("America/Los_Angeles").strftime('%A %B %d, %Y at %H:%M') %> PST.</em>
    </p>
    <%
      items = site.items.latest.limit(24)
      ItemCursor.new( items ).each do |item, new_date, new_feed|
    %>
      <h3>
        <%= link_to item.title, item.url %>
      </h3>
      <p>
        <%= link_to item.published.strftime('%A, %d. %B %Y'), item.feed.url %> | 
        <% if item.content %>
          <%= textify( item.content ) %>
        <% elsif item.summary %>
          <%= textify( item.summary ) %>
        <% else %>
          -/-
        <% end %>
     </p>
    <% end %>
    <footer>
      Powered by <a href='https://github.com/feedreader'>Pluto</a> | Based on a tempate <a href="https://werd.io" rel="me">by Ben Werdmuller</a>
    </footer>
  </body>
</html>
